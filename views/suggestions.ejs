<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Suggested Goals</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f5f5f5;
    }
    .card {
      border-radius: 15px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
      margin-top: 50px;
    }
    .btn-primary {
      background-color: #004d00;
      border: none;
    }
    .btn-primary:hover {
      background-color: #003300;
    }
    .navbar {
      background-color: #004d00;
    }
    .navbar-brand, .nav-link, .navbar-toggler-icon {
      color: #fff !important;
    }
  </style>
</head>
<body>
  <nav class="navbar navbar-expand-lg">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Sustainable Living Tracker App</a>
    </div>
  </nav>
  <div class="container">
    <div class="card p-4">
      <h3 class="text-center mb-4" style="color: #004d00;">Suggested Goals</h3>
      <p>Based on your responses, here are some suggested goals you can add:</p>
      <ul class="list-group">
        <% if (suggestions.length > 0) { %>
          <% suggestions.forEach(function(suggestion) { %>
            <li class="list-group-item"><%= suggestion %></li>
          <% }); %>
        <% } else { %>
          <li class="list-group-item">No suggestions available based on your responses.</li>
        <% } %>
      </ul>
      <div class="d-grid gap-2 mt-4">
        <a href="/addgoal" class="btn btn-primary">Add Goal</a>
        <a href="/" class="btn btn-secondary">Go to Dashboard</a>
      </div>
    </div>
  </div>
</body>
</html>
