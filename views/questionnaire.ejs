<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Questionnaire</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f2f2f2;
    }
    .questionnaire-container {
      margin-top: 50px;
    }
    .card {
      background-color: white;
      border-radius: 10px;
      padding: 20px;
      box-shadow: 0px 0px 10px 0px rgba(0,0,0,0.1);
    }
  </style>
</head>
<body>
  <div class="container questionnaire-container">
    <div class="card mx-auto" style="max-width: 600px;">
      <h3 class="text-center mb-4" style="color: #004d00;">Lifestyle Questionnaire</h3>
      <form action="/submit-questionnaire" method="POST">
        <div class="mb-3">
          <label class="form-label">How often do you use plastic bags?</label>
          <select class="form-control" name="plastic_bags">
            <option value="always">Always</option>
            <option value="sometimes">Sometimes</option>
            <option value="never">Never</option>
          </select>
        </div>
        <div class="mb-3">
          <label class="form-label">How often do you recycle?</label>
          <select class="form-control" name="recycle">
            <option value="always">Always</option>
            <option value="sometimes">Sometimes</option>
            <option value="never">Never</option>
          </select>
        </div>
        <div class="mb-3">
          <label class="form-label">How often do you use public transport?</label>
          <select class="form-control" name="public_transport">
            <option value="always">Always</option>
            <option value="sometimes">Sometimes</option>
            <option value="never">Never</option>
          </select>
        </div>
        <div class="mb-3">
          <label class="form-label">Do you have a garden where you can plant trees?</label>
          <select class="form-control" name="garden">
            <option value="yes">Yes</option>
            <option value="no">No</option>
          </select>
        </div>
        <div class="d-grid gap-2">
          <button type="submit" class="btn btn-primary" style="background-color: #004d00;">Submit</button>
        </div>
      </form>
    </div>
  </div>
</body>
</html>
