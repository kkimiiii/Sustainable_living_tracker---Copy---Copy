<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <title>Sustainable Living Tracker App</title>
    </head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">Sustainable Living Tracker App</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="collapsibleNavbar">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link" href="/addGoal">Add new Goal </a>
              </li> 
            </ul>
          </div>
        </div>
    </nav>

    <div class="container">
        <br>
        <div class="text-center"><h2>Update Goal</h2></div>
        <br>
        <div class="text-center">
          <% if (goal) { %> 
            <form action="/editGoal/<%= goal.goalId %>" method="POST">
                <label for="Description">Description:</label><br>
                <input type="text" id="Description" name="Description" value = <%= goal.goalDescription %> required><br><br>

                <label for="StartDate">StartDate:</label><br>
                <input type="date" id="StartDate" name="StartDate" min="0" step="1" value = <%= goal.StartDate %> required><br><br>

                <label for="TargetDate">TargetDate</label><br>
                <input type="date" id="TargetDate" name="TargetDate" min="0" step="0.01" value = <%= goal.TargetDate %> required><br><br>


                <button type="submit">Update Goal</button>
            </form>
          <% } else { %>
              <p>No goal found.</p>
          <% } %>
          <a href ="/">Back</a>
        </div>
    </div>
</body>
</html>
